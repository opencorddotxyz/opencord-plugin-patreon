export const images = (name: string) => '/assets/images/' + name;
export const icons = (name: string) => '/assets/images/icons/' + name;
export const placeholders = (name: string) =>
  '/assets/images/placeholders/' + name;
