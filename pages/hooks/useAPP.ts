import { useInit } from '@/utils/store/useStore';

export const useAPP = () => {
  // todo init app
  useInit(async () => {
    // todo login app
    // todo check isAdmin
    // todo check isConnected
  });
};
